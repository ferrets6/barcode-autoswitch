<Window x:Class="BarcodeAutoSwitch.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wpf="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        xmlns:cef="clr-namespace:CefSharp;assembly=CefSharp.Core"
        xmlns:behaviours="clr-namespace:CefSharp.MinimalExample.Wpf.Behaviours"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" 
        Title="{Binding WindowTitle, Converter={StaticResource TitleConverter}}"
        Icon="icon.ico"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="2,5" />
                </Style>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="Margin" Value="2,5" />
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtBoxAddress" Grid.Column="1" Text="{Binding Address, ElementName=Browser}"  FontSize="12" BorderBrush="Gray" BorderThickness="1" IsEnabled="False">
                <i:Interaction.Behaviors>
                    <behaviours:TextBoxBindingUpdateOnEnterBehaviour />
                </i:Interaction.Behaviors>
            </TextBox>
            <Separator Grid.Column="2" />
            <Button Grid.Column="3" Content="Cambia porta COM" Click="ChangeCOMPortButton"  Width="Auto" />
        </Grid>
        <Grid Name="backgroundGrid" Grid.Row="1" Panel.ZIndex="0">
            <TextBlock TextAlignment="Center" FontSize="30" Margin="0,0,-0.4,276.2">
                <LineBreak/>
                Porta COM non valida.
                <LineBreak/>
                <LineBreak/>
                <Run FontSize="20">Selezionare una porta COM valida, utilizzando l'apposita funzione</Run>
            </TextBlock>
            <Button Content="Cambia porta COM" Click="ChangeCOMPortButton" Width="200" Height="50" TextBlock.FontSize="18"/>
        </Grid>
        <Grid Grid.Row="1" Name="browserGrid" Panel.ZIndex="1">
            <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="0,1">
                <wpf:ChromiumWebBrowser x:Name="Browser"
                                    Address="www.google.com">
                    <i:Interaction.Behaviors>
                        <behaviours:HoverLinkBehaviour x:Name="HoverLinkBehaviour"/>
                    </i:Interaction.Behaviors>
                </wpf:ChromiumWebBrowser>
            </Border>
        </Grid>
        <ProgressBar IsIndeterminate="{Binding IsLoading, ElementName=Browser}"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Top"
                     Width="Auto"
                     Grid.Row="2"
                     Height="2"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, ElementName=Browser}"
                     BorderThickness="0" />
        <StatusBar Grid.Row="2" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0">
                <Button Name="btnShowHideConsole" Content="Show Console" Grid.Column="0" Click="btnShowHideConsole_click" Width="100" />
            </StatusBarItem>
            <StatusBarItem Grid.Column="1" HorizontalAlignment="Right">
                <Button Name="btnEnableDisable" Content="Disattiva" Grid.Column="0" Click="btnEnableDisable_click" Width="100" />
            </StatusBarItem>

            <!--<StatusBarItem Grid.Column="1">
                <TextBlock Text="{Binding HoverLink, ElementName=HoverLinkBehaviour}" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
            </StatusBarItem>-->
            <StatusBarItem Name="statusLabelContainer" Grid.Column="2" Background="Green" HorizontalAlignment="Right">
                <TextBlock Name="statusLabel" HorizontalAlignment="Center" FontWeight="Bold" TextAlignment="Center" TextBlock.FontSize="15" Height="18" Width="150">Attivo</TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
